version: "1"
rules:  # Array of rules
  - base: master   # Required. Target branch
    upstream: choreonoid_ros:master    # Required. Must be in the same fork network.
    mergeMethod: hardreset  # Optional, one of [none, merge, squash, rebase, hardreset], Default: none.
#  - base: master_irsl
#    upstream: choreonoid_ros:master
#    mergeMethod: merge
#  - base: stable_irsl
#    upstream: IRSL-tut:stable
#    mergeMethod: merge
## irsl_patch
  - base: devel
    upstream: choreonoid_ros:master
    mergeMethod: merge
### [TODO] add stable
